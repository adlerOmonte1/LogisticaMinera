from django.apps import AppConfig


class SalidasConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.salidas"
    verbose_name = "M04 — Salidas y movimientos"
